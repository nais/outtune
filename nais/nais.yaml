apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: "outtune-api"
  namespace: "naisdevice"
  labels:
    "team": "naisdevice"
spec:
  image: {{image}}
  liveness:
    path: "/isalive"
    port: 8080
    initialDelay: 2
    timeout: 2
  readiness:
    path: "/isready"
    port: 8080
    initialDelay: 2
    timeout: 2
  replicas:
    min: 1
    max: 1
    cpuThresholdPercentage: 50
  prometheus:
    enabled: false
    path: "/metrics"
  limits:
    cpu: "200m"
    memory: "256Mi"
  requests:
    cpu: "20m"
    memory: "32Mi"
  accessPolicy:
    outbound:
      external:
        - host: something.gcloud.com
  ingresses:
    {{#each ingresses as |url|}}
       - {{url}}
      {{/each}}
